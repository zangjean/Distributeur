# Distributeur

Prérequis:
    Java JDK 21 installé (ex. Zulu, OpenJDK…)
    Maven installé (mvn -v pour vérifier)
    Git (optionnel)

Lancer l'application via Maven

Dans un terminal, à la racine du projet :
    mvn clean compile
    mvn javafx:run
