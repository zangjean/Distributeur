package application.model.distrib.productModel.product.beverage.sugar.all.chocolate;

/**
 * Enumération représentant les toppings disponibles pour un chocolat chaud.
 * Chaque topping ajoute un coût supplémentaire de 0.20 €.
 */
public enum Topping {

    /** Topping : marshmallow. */
    MARSHMALLOW,

    /** Topping : chantilly. */
    CHANTILLY,

    /** Topping : spéculoos. */
    SPECULOS,

    /** Topping : morceaux d'Oreo. */
    OREO
}
